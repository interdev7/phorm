name: phorm_benchmarks
description: Cross-ORM benchmarks for PHORM (not published).
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.28.0
  phorm_sqlite:
    path: ../packages/phorm_sqlite
  sqlite3: ^3.3.4

dev_dependencies:
  build_runner: ^2.4.9
  drift_dev: ^2.28.0
  flutter_test:
    sdk: flutter
