name: phorm_example
description: Example app for PHORM.
publish_to: "none"
version: 1.0.0+1
# resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  path: ^1.9.0
  phorm_sqlite: ^1.0.0
  phorm_generator: ^1.0.0
  uuid: ^4.5.2

dev_dependencies:
  build_runner: ^2.0.0
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
